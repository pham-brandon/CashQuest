<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/purple_background"
    android:padding="8dp">

    <!-- Goal Icon -->
    <ImageView
        android:id="@+id/goal_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:contentDescription="@string/exp"
        android:src="@drawable/star_goal_24dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Goal Description -->
    <TextView
        android:id="@+id/goal_desc"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:fontFamily="@font/retro_font"
        android:text="@string/goal_desc"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toStartOf="@id/goal_exp"
        app:layout_constraintStart_toEndOf="@id/goal_icon"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Date directly below the Goal Description -->
    <TextView
        android:id="@+id/goal_exp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="24dp"
        android:fontFamily="@font/retro_font"
        android:text="@string/goal_exp"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/goal_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:fontFamily="@font/retro_font"
        android:text="@string/goal_date"
        android:textColor="@android:color/white"
        android:textSize="14sp"
        android:textStyle="italic"
        app:layout_constraintStart_toStartOf="@id/goal_desc"
        app:layout_constraintTop_toBottomOf="@id/goal_desc" />
</androidx.constraintlayout.widget.ConstraintLayout>
